
public enum HeroType
{
    Melee,
    Ranged
}

public enum SkillType
{
    Attack,
    Defense,
    Magic
}

public enum ActionsType
{
    Attack,
    Defense,
    Move,
}

public enum GameStates
{
    Playing,
    SelectingAction,
    PerformingAction,
    Waiting,
}
